pipeline{
    agent{
        docker {image 'node:16-alpine'}
    }
    stages{
        stage('tet'){
            steps{ 
                sh 'docker version'           
            }

            }
    }
}